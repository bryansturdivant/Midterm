<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Nerds - Discussion</title>
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    {{> header}}

    <main class="container">
        <div class="comments-container">
            <h1>Discussion Board</h1>
            <p class="page-subtitle">Join the conversation with fellow book lovers</p>

            <div class="comments-section">
{{!This is where we begin our handle bar 'loop' with Each and our comments object}}
                {{#each comments}}               
                <div class="comment-card">
                    <div class="comment-header">
                        <div class="author-info">
                            
                            <div class="author-details">
                                <h3 class="author-name" style="color:{{profile_color}}">{{display_name}}</h3>
                                <span class="comment-date">{{date}}</span>
                            </div>
                        </div>
                    </div>
                    <div class="comment-body">
                        <p>{{comment}}</p>
                    </div>
                </div>
                {{/each}}

            </div>
        </div>
    </main>

    {{> footer}}
</body>
</html>